<h1>Competitions Index</h1>

<ul>
  <% @competitions.each do |competition| %>
    <section id="comp-<%= competition.id %>">
      <li><%= link_to "#{competition.name}", competition_path(competition) %></li>
    </section>
  <% end %>
</ul>
